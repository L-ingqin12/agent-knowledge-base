---
title: CORRECTIONS — 错误记忆库（可检索）
aliases: [CORRECTIONS, 错误记忆, 教训库, 踩坑记忆, error memory, corrections log]
tags: [meta, ai/ops, incident]
created: 2026-09-12
updated: 2026-09-22
status: review
---

# CORRECTIONS — 错误记忆库

See also: [[AGENTS]] | [[HOME]] | [[Claude-Ops-KB-Home]] | [[URL-Lookup]] | [[agent-memory-context-knowledge-design]] | [[DSH会话脱敏插件缺陷档案]] | [[DSH会话脱敏项目方法论复盘]] | [[DSH插件发布与分发]]

> [!abstract] 这个文档是什么
> **可检索的方法论错误记忆库**。记录的不是「知识」，而是**做过的错误判断、错在哪一步、以及可复用的判定规则**。
>
> 为什么单独建库：本库已有 [[MEMORY-INDEX]]（架构模式记忆）与 31 处散落的「教训」小节，但**没有一处专门记录「我判断错过的结论」**。错误散落在正文里 = 不可检索 = 必然重犯。
>
> 分层定位：按 [[agent-memory-context-knowledge-design]] 的三级模型，本库属 **L3 长期记忆**。**只把触发规则放进提示词，正文留在本文件**——该文档明确警告「把长期知识硬编码进系统提示词是最高频反模式」。

> [!danger] 录入门槛（不记琐碎错误）
> 只有满足以下**至少一项**才录入：
> 1. 错误**已输出给用户或写入交付物**，可能被当真
> 2. 错误属于**可复现的推理模式**，换个场景还会再犯
> 3. 错误**推翻了一个原本正确或原本错误的结论**，导致结论反复
>
> 拼写、笔误、一次性疏漏不录。

---

## 速查索引（按「什么时候要回忆」检索）

| 当你准备… | 警戒 | 条目 |
|---|---|---|
| 判定某版本「是否受某漏洞影响」 | 只读了「受影响范围」，**必须同时读「修复版本」** | [[#C-001 只读漏洞公告的一半]] |
| 把「开放端口 / 可达 / 存有凭据」写成风险 | **可达 ≠ 可被利用**；先确认认证方式与凭据强度 | [[#C-002 把可达性外推为可被利用]] |
| 用脚本或工具扫一遍就下「干净」结论 | **扫描器可能是瞎的**；先验证扫描本身有效 | [[#C-003 信任未经校准的扫描结果]] |
| 引用他人（含子代理）的结论 | 对照原始来源复核，尤其**结论数字与计数** | [[#C-004 转述未回溯原始来源]] |
| 改动「别人要读的文件」（日志/配置/产物） | **自检通过 ≠ 读取端会接受**；判据要用对方自己的校验 | [[#C-005 拿工具自检当读取端验收]] |
| 为命令输出设计多行排版 | **渲染端先压平再截断**（控制字节→空格、200 显示格），多行必成一行 | [[#C-006 假设渲染行为而不读渲染端]] |
| 用 `--dump-config` 证明插件能加载 | **它只做配置组合，从不 import 模块**；不存在的包名也照样退出 0 | [[#C-007 拿配置组合当运行时证明]] |
| 用「测试全绿」当「工具可用」的依据 | **夹具形状决定断言的意义**；夹具不是真实产物形状时，全绿也是盲区 | [[#C-008 拿绿色测试当可用性证据]] |
| 看到服务存在就断定效果会出现 | **服务可用 ≠ 效果可达**；准入守卫与挂载点优先级都能让效果消失 | [[#C-009 把服务存在外推为效果出现]] |
| 用条件字段让配置行自动二选一 | **它可能是实时 getter**；判据随挂载进度翻转，可把整个 boot 拖挂 | [[#C-010 把配置字段当作稳定值]] |
| 把参数设成 0 表示「不要这个字段」 | **先测边界值**；截断函数在预算 0 时会留下一个 `…` 占位符 | [[#C-011 只测常规值不测边界]] |
| 本机跑绿就准备推送 / 发布 | **平台专有假设在写它的平台上隐形**；判据是「在目标平台上跑过」，不是「本机跑过」 | [[#C-012 拿本机绿测当跨平台验收]] |
| **写「关于脱敏/安全的说明」文档** | **不要把被脱敏的值当例证写进去**——文档自身也会公开 | [[#C-013 为讲清教训而把敏感字面量写进会公开的文档]] |
| 设计或维护检查工具（扫描/门禁/lint） | **误报会淹没真问题**；豁免必须显式、可审计、最小化 | [[#C-014 检查工具只加模式不加豁免，被误报淹没]] |
| 用一条 `grep`/`filter` 扫完就断言「X 不存在」 | **先怀疑过滤器**：它可能把待查对象 100% 筛掉了 | [[#C-015 把过滤器筛空的零命中当成「不存在」]] |
| 断言某扫描器/校验器「会漏掉 / 会误导」 | **去读那个扫描器的实现**；方向可能与推断正相反 | [[#C-016 凭推断给扫描器定行为，方向反了]] |
| 指认某处实现「与库内其它地方不一致」 | **先读对方源码**：写法不同常常是**对的** | [[#C-017 把「写法不同」当成「不一致」]] |
| 说「官方模板/默认配置带了某项保护」 | **去读实际值**——「应该有」不等于「已有」 | [[#C-018 把「应该有」当成「已有」]] |
| 看到提交表单 / 入口就认为通道可用 | **表单在 ≠ 服务在**；先做一次真实提交或看现有记录反证 | [[#C-019 把「表单存在」当成「提交通道可用」]] |
| 用 `--label`、模板字段等「工具会自动带上」的方式提交 | **退出码 0 ≠ 副作用生效**；创建后用独立读操作回查关键字段 | [[#C-020 把「命令退出 0」当成「副作用已生效」]] |
| 用 API/CLI 代替 issue 表单提交 | **表单是自动化接口**：它按 `### 字段名` 解析正文，自由 markdown 会报字段缺失 | [[#C-021 把 issue 表单当成给人看的渲染层]] |
| 把冻结快照 / 历史副本留在仓库里 | **副本会被工具当成真货**：README 里"不可用"的标注机器读不到 | [[#C-022 把「人知道它不可用」当成「工具知道」]] |
| 拿一个数字 / 计数 / 均值推断出机制 | **代理量 ≠ 目标量**；先问「它在量什么」「是谁产生的」 | [[#C-023 拿一个代理指标外推出机制，并当作结论说出]] |
| 静态审查、单测全绿就准备上线**观测代码** | **上线后先核对产出的形状**（频率 / 缺失 / 退化），别先看结论 | [[#C-024 拿静态审查的绿灯当「真实流量下无缺陷」的证据]] |
| 做性能结论 / 排查「为什么卡」 | **先做进程普查**；清掉自己留下的孤儿进程**再复测** | [[#C-025 自己留下的失控进程污染了自己的全部测量]] |
| 要引用一个速度/性能数字 | **至少跑两轮**；两轮不一致时先查环境，**别先编机制** | [[#C-026 拿单次测量当可复现结论]] |
| 观测到「被测对象好像有缺陷」 | **先怀疑自己的工具链**；换一条独立路径复现，消失即是自己的问题 | [[#C-027 把自己工具的缺陷当成被测对象的缺陷]] |
| 加了防缓存 / 防污染的手段 | **反验它生效了**；用硬件上限做量纲校验，超上限=测量无效 | [[#C-028 基准测量被服务端缓存污染（防缓存手段本身没验证）]] |
| 排查一个故障时想到一个「能解释症状」的原因 | **一次只改一个变量**；改完复测它是否真的影响了目标值，没影响就立刻撤回**别叠加** | [[#C-029 对一个故障连续三次把「能解释症状的单一现象」当成根因]] |
| 得到一个「负结果」准备下结论时 | **先验证两次实验的装置等价**（如日志里的 free=）；装置不等价则结论作废 | [[#C-030 拿被自己污染的实验得出的负结果当结论——并已写入交付物]] |

> [!tip] 一句话总纲
> 本库 **32 条**错误（截至 2026-09-22）同属一个根源：**把「观察到的现象」直接当成「结论」**。
> 现象 → 需要一步判定 → 才能成为结论。**漏掉中间那一步，就是这三十二条。**
> 五个变体：
> · **C-001~C-004** 把**外部信息**当成结论（公告读一半 / 可达当可利用 / 扫描器当眼睛 / 转述当原文）；
> · **C-005~C-012** 把**自己这一侧的通过**当成**对方的验收**（自检当读取端 / 配置组合当加载 / 绿测当可用 / 服务当效果 / 设 0 当删除 / 本机绿测当跨平台验收）；
> · **C-013~C-014** 把**「说明」与「材料」当成两个世界**——写文档时引用被脱敏的值（C-013）、设计门禁时只顾检出不顾豁免（C-014）。两条都在讲：**约束必须覆盖「我自己产出的东西」**；
> · **C-015~C-022** 把**推断或一端的结果**当成**证据**（零命中当不存在 / 猜的机制当事实 / 写法不同当不一致 / 应该有当已有 / 表单在当通道通 / 退出 0 当副作用生效 / 正文当字段 / 文档标注当机器可读）——八条缺的都是同一个动作：**去读实际的那一侧**。
> · **C-026~C-028** 把**自己的测量或自己的工具**当成**客观事实**（单次测量当可复现 / 自己工具链的缺陷当成被测对象的缺陷 / 未经验证的防污染手段当成"干扰已排除"）——三条缺的都是同一个动作：**在相信自己这一侧之前，先反验一次**。
> 其中 C-015 与 C-003 同族（扫描器是瞎的，只是这次瞎的是过滤器）；C-020~C-022 与 C-005~C-012 同族（把一端的"成功/完整"当成另一端的事实）；C-020 本身还贡献了一条元规则：**回查到的异常是线索，不是结论**（机制为真、因果为假）。C-026~C-028 是第五变体的首次成组录入——三条全部发生在**同一次**本地 LLM 实测中，说明**测量类工作需要一个独立于"得出数字"的复核环节**。

---

## C-001 只读漏洞公告的一半

| 项 | 内容 |
|---|---|
| 发生 | 2026-09-12 |
| 场景 | 核对 `path-to-regexp` 8.4.2 是否受 CVE-2026-4926 影响 |
| 我的错误结论 | 「该版本受影响，且**无补丁可用**」→ 据此判定子代理「漏报一条高危」 |
| 事实 | 公告同页写着 `Patched version: >= 8.4.0`；8.4.2 **已修复**。Debian 与 Ubuntu 追踪库均确认 `Fixed in version 8.4.0` |
| 错在哪一步 | 读了 `Affected versions: >= 8.0.0` 就停止阅读，**没读紧随其后的下一行** |
| 可复用规则 | **判定受影响 = `已装版本 ∈ 受影响范围` 且 `已装版本 < 修复版本`。两个值缺一不可。** |
| 检测方法 | 写完任何漏洞判定，自查：「我引用的修复版本号是多少？从哪一行读到的？」答不出即无效 |
| 波及 | 向用户输出过两次相反结论；已更正并保留记录 |

> [!bug] 加重情节
> 我不仅自己判错，还**推翻了子代理原本正确的结论**。复核他人结论时，若自己的复核依据不完整，会把「对的改成错的」。

---

## C-002 把可达性外推为可被利用

| 项 | 内容 |
|---|---|
| 发生 | 2026-09-12 |
| 场景 | 检视本机 MobaXterm 会话记录，发现 4 个公网 IP 的 `root` SSH 会话 |
| 我的错误结论 | 报为**严重项「S8」**：3 台公网主机 22 端口可达 + root 直登 + 凭据存于无主密码的 MobaXterm |
| 事实 | 用户确认**密钥认证、无口令复用**；复检 `MobaXterm.ini` **无任何口令字段**，其私钥目录**无私钥文件**。原判断不成立，已撤回 |
| 错在哪一步 | 三步缺失：① 只测了 TCP 可达，**没验证认证方式**；② 把「21 条会话」误读为「21 条口令」（实际是会话元数据）；③ **没先问所有者**就列入严重项 |
| 可复用规则 | **判定暴露面必须落到三个具体问题：认证方式是什么？凭据强度如何？是否复用？** 三者缺一，就只是「观察」而非「判断」 |
| 补充 | 「公网开放 22 + 使用 root」是**常规配置**，不是缺陷。`root` 是标准用户名，不泄露信息 |
| 元教训 | 过度定级会**稀释真正严重项的可信度**——把常规配置报成严重，读者会开始怀疑整份报告 |

> [!quote] 用户的纠正是对的
> 「root 登录应该本身不构成威胁吧，除非别人入侵了我的电脑」——这个推理链成立。**root 直登 vs 普通用户 + sudo 的风险差异很小**（拿到任一账户后 `sudo bash` 一行即达），业界主张禁用主要为**可审计性**，非漏洞。

---

## C-003 信任未经校准的扫描结果

| 项 | 内容 |
|---|---|
| 发生 | 2026-09-12 |
| 场景 | 审计 `~/.dsh/profiles/node_modules` |
| 风险 | 该路径是 **256 个 junction 的农场**，`Get-ChildItem -Recurse` 与 `rg`/`grep` **都不跟随 junction** |
| 实测 | `-Recurse -File` 返回 **0 个文件**（而目录有 628 个） |
| 后果（若未发现） | 任何密钥扫描/依赖审计都会返回「0 命中」，并**被误读为「干净」** |
| 可复用规则 | **下「未发现」结论前，先证明扫描器看得见目标。** 用一个已知存在的样本做阳性对照 |
| 检测方法 | 扫描后问：「我期望至少命中几条？没有命中是好事还是扫描器瞎了？」 |
| 延伸 | 同理适用于：`.gitignore` 排除、权限不足导致的静默跳过、大小写/编码差异 |

---

## C-004 转述未回溯原始来源

| 项 | 内容 |
|---|---|
| 发生 | 2026-09-12 |
| 场景 | 采纳子代理的依赖 CVE 审计结论 |
| 现象 | 子代理把 `ws` 的 **CVE-2026-48779 与 GHSA-96hv-2xvq-fx4p 列为两条不同公告**，实际**是同一个**（GHSA 是 GitHub 侧编号，CVE 是通用编号） |
| 另一现象 | 子代理只查了它自己列出的 CVE 编号，**没有反向核对上游项目的官方安全公告**（Express blog 同批就披露了 3 条 CVE） |
| 可复用规则 | 采纳转述结论时：① **核对计数**（「2 条」是不是真的是 2 条不同的事）；② **回溯到项目官方来源**，不只依赖聚合站编号列表 |
| 边界 | 这不意味着不要用子代理——而是**转述必须留下可回溯的原始链接** |

> [!note] 与源码的关系
> 本条不是「子代理不可信」，而是「**转述衰减**」：每经一手，计数与范围都可能漂移。同一份报告里，我也犯了 C-001（自己判错）。

---

## C-005 拿工具自检当读取端验收

| 项 | 内容 |
|---|---|
| 发生 | 2026-09-12 |
| 场景 | 设计 `dsh-plugin-redact` 的删行能力：删会话日志（`session.vN.jsonl.zstd`）中间行是否安全 |
| 我的错误结论 | 「删行是安全的通用能力——工具自己的自检（JSON 可解析、头部合法、`seq` 密集）通过，日志就是好的」→ 据此把 `dropLines` 当常规手段写进文档与流程 |
| 事实 | 读取端逐行断言 `event.seq === eventCount`（`seq` 必须等于 0-based 行号）。**删掉中间任意一行 = 整份日志被判损坏 = 会话根本打不开**，不是「少一行」（真实后端实测 `SessionPersistenceCorruptionError`）。安全面只有两个：原地改写（行数/`seq` 全不变）与**后缀**删除（引用一律向后指）；删中间行必须显式 `renumber` 重编号并重映射引用 |
| 错在哪一步 | 从「我这边的自检通过」**直接推广成「读取端会接受」**；中间缺的那一步是**去读读取端的校验代码**——自检清单只覆盖了本工具自己关心的性质 |
| 可复用规则 | **「我检查通过」与「对方会接受」是两件事。** 凡是改动「别人要读的文件」，判据必须是**对方自己的校验**，不是自己的检查清单 |
| 检测方法 | 落结论前问：「这条不变量写在读取端的哪个文件哪一行？」答不出 `文件:行号`，就说明还没验证过 |

---

## C-006 假设渲染行为而不读渲染端

| 项 | 内容 |
|---|---|
| 发生 | 2026-09-12 |
| 场景 | 设计 `/redact` 各子命令在 TUI 里的结果排版（清单、备份路径、统计想分行显示） |
| 我的错误结论 | 「输出字符串里写 `\n`，用户在屏幕上就会看到多行」 |
| 事实 | dsh-tui **不直接显示**命令返回的 `text`：先过 `cleanRenderText(text, 200)`（`Chat.js:120` 常量、`:1082` 调用点，实现在 `dsh-adapter/sanitize.js`）——先把 `[\x00-\x1f\x7f-\x9f]`（含 `\n`、`\t`）全部替换成空格、再把连续空白压成一个空格，然后按 **200 显示格**截断并补 `…`（CJK 算 2 格）。**多行必被压成一行，超出必被砍尾**，绕不过去 |
| 错在哪一步 | 按「输出该长什么样」的直觉做设计，**没有去读渲染端**。发现时两轮排版工作已作废，所有输出推倒重来：单行 + ` · ` 分隔 + 最重要信息在最前 |
| 可复用规则 | **输出格式是消费端的属性，不是生产端的自由度。** 面向 UI 的输出，先读它的压平/截断代码，再决定排版 |
| 检测方法 | 找到消费这段文本的那个函数，读它的替换正则与截断常量；说不出函数名与常量值，就说明还没读过 |

---

## C-007 拿配置组合当运行时证明

| 项 | 内容 |
|---|---|
| 发生 | 2026-09-12 |
| 场景 | 用 `dsh --profile <name> --dump-config` 确认插件挂载（写进 README §2.4 与安装检查清单） |
| 我的错误结论 | 「dump 里出现 `id: dsh-redact` 这一行 ⇒ 插件能加载、命令能注册」 |
| 事实 | `--dump-config` **从不 import 插件模块**，只做配置层的组合（`applyEntryPatches`）。实测：补丁里插入一行 `name: dsh-plugin-totally-nonexistent-xyz`（`boot-verify\bogus-patch.yml`），dump 照样打出该行、进程照样**退出码 0**；只有真 boot 才会在 import 阶段报 `Cannot find package 'dsh-plugin-does-not-exist-xyz'` |
| 错在哪一步 | 把**配置组合阶段的检查当成运行时检查**：把「这行出现在合成结果里」读成「这行会被加载」，跳过了「组合 → import → apply → ACTIVE」这条链路 |
| 可复用规则 | **配置检查只证明配置，不证明加载。** 要证明插件真的起来，判据是启动后的行状态（ACTIVE）与它注册出来的命令，不是 dump 文本 |
| 检测方法 | 用一条**明知会失败**的行（不存在的包名 / import 即抛错的模块）跑同一个检查；它若照样「通过」，这个检查就不能用来证明成功 |

---

## C-008 拿绿色测试当可用性证据

| 项 | 内容 |
|---|---|
| 发生 | 2026-09-12 |
| 场景 | 以 `dsh-plugin-redact` 三套自测（`handler.selftest.mjs` 159 / `bugfix-regression.mjs` 48 / `session-surgery\selftest.mjs` 34，共 241 条全绿）作为「工具可用」的依据，并写进交付物 |
| 我的错误结论 | 「三套全绿 ⇒ 工具工作正常、输出能被打开」 |
| 事实 | `bugfix-regression.mjs` 的夹具**不是读取端合法日志**：第 30/142 行把 tool/result 写成 `{ role:'tool', source:{callId:'aa'} }`，真实形状是 `role:'user'` + `source:{kind:'tool',callId}`。用真实 `JsonlSessionPersistence.open()` 一跑就报 `session event at seq 4 message must have role "user"` / `has invalid source`。**绿色断言对「读取端能否打开输出」零信息量**；而红队找到的两条不可逆缺陷（F1 `sourceEventSeqs` 游程未重映射、F2 `session/title.messageSeqs` 悬空）恰好都住在这个盲区里 |
| 错在哪一步 | 把「我的断言全过了」当成「系统正确」，**没问「这套测试会漏掉什么」与「我的夹具和真实产物同形状吗」** |
| 可复用规则 | **测试的可信度上限 = 夹具与真实产物的同形度。** 改动别人的产物时，判据必须是**真实消费者**（真实读取端/后端），并至少留一条「真实消费者能打开输出」的断言 |
| 检测方法 | ① 每条断言问一次「它为假时，是夹具错了还是实现错了？」② 把夹具喂给真实消费者跑一遍：绿着却被消费者拒绝 = 夹具在说谎 |

---

## C-009 把服务存在外推为效果出现

| 项 | 内容 |
|---|---|
| 发生 | 2026-09-12 |
| 场景 | `/redact pick` 面板：以「`ctx.get('tuiDialogs')` 有值 ⇒ 面板会弹」为前提设计交互与配置说明 |
| 我的错误结论 | 「对话框服务存在（且请求被接纳），面板就会出现」 |
| 事实 | 被证伪两次。**(a) 准入守卫**：`tuiDialogs` 运行时在 `try/catch` 之外还有一道准入（`dsh-adapter/dialogs.js:103-111`，`bindOwnerEffect` 失败即 `pending.onAbort()`）。本行若早于 composition-root tracker 建立而进入 ACTIVE，每次 `select` / `confirm` 只写一条 logger warning 就返回取消值——**面板从不渲染，用户只看到「已取消」**（加行级 `inject` 后实测从 0/6 变 6/6）。**(b) 挂载点被压掉**：面板**唯一**的实例化点是 `Chat.js:3585`，而 `approvalPanelNode` 在三元链里排在它前面、无提示压掉它；此时键盘让出（`Chat.js:2566`）已经生效、approval 又没有超时——**服务在、请求挂在 store 里、键盘已交出、面板永不渲染**（根因报告 §2.2 矩阵：7 个死锁态，A1 只要一个 approval 挂起就成立） |
| 错在哪一步 | 验证停在「服务可用」这一层，**没有验证「服务调用 → 效果可见」整条链路**：准入、挂载点优先级、键盘归属是三道各自独立的闸门，任何一道关闭，效果就不存在 |
| 可复用规则 | **服务存在 ≠ 效果可达。** 依赖 UI 效果时，要么把「效果真的出现」当判据，要么换成**结构上不可能失败**的路径（最终改成不把 promise 停进 store 的两段式命令 `nodes` → `hide <序号> --commit`） |
| 检测方法 | 把「服务在」与「效果在」当两次独立观测；只要两者之间存在任何带优先级的挂载/准入分支，就按最坏顺序逐条走一遍 |

---

## C-010 把配置字段当作稳定值

| 项 | 内容 |
|---|---|
| 发生 | 2026-09-12 |
| 场景 | 为 `dsh-redact` 行写「有 `tuiDialogs` 提供者就启用、没有就跳过」的 `!!js disabled` 自禁用守卫，想让两种 profile 自动二选一 |
| 我的错误结论 | 「`disabled` 是个布尔字段，写个判据就能自动二选一」 |
| 事实 | `Entry.disabled` 是**实时 getter**，每次读取都重新求值。判据（`![...ctx.loader.entries()].some(e => e.options.name === '…' && !e.disabled)`）依赖挂载进度，同一行会在 boot 过程中从「被跳过」翻成「被算作加载失败的行」；启动器把未激活/PENDING 的行升级为致命错误——**整个 boot 中止**（提供者排在后面时必炸），不是单行静默降级 |
| 错在哪一步 | 把配置字段当**稳定值**读，而它其实是**表达式**；没有问「这个判据在 boot 的哪一刻求值、那一刻输入就绪了吗」 |
| 可复用规则 | **配置里能求值的东西，都要问「什么时候求值、求值几次」。** 判据依赖其它行的挂载状态时，不能用来决定本行是否加载；要二选一就用**静态的两层**（TUI 层与非 TUI 层），不要用运行时判据 |
| 检测方法 | 对任何 `!!js` / 条件字段，指出它的求值时刻与当时的输入状态；做不到就改设计 |

---

## C-011 只测常规值不测边界

| 项 | 内容 |
|---|---|
| 发生 | 2026-09-12 |
| 场景 | 给 `argsCells`（命令行显示格预算）留隐私开关，文档声明「`0` = 完全不显示命令行，连 `…` 都不会有」 |
| 我的错误结论 | 「预算设成 `0` 就等于这个字段不存在」 |
| 事实 | 截断函数 `clamp(s, budget)` 在 `budget = 0` 时循环第一步就 `break`，返回 `'' + '…'` —— **一个只剩 `…` 的占位符**。于是这个「隐私开关」反而在清单文件与面板里留下痕迹，只有调用点外层的 `argsCells > 0` 把它盖住。修法是在 `clamp` 里对 `budget <= 0` 短路返回 `''`（注释就在 `index.js:42-44`） |
| 错在哪一步 | 只按**常规值**（默认 120、自定义值）验收，**没有把参数推到边界**（`0` / 负数）；把「文档里写了 `0` 的含义」当成「`0` 的行为已验证」 |
| 可复用规则 | **凡是「设为 0 / 空 / false 即关闭」的开关，必须单独测这个边界值**，判据落在成品上（产物里到底有没有那个字符），而不是「分支有没有走到」 |
| 检测方法 | 对每个「关闭档」参数跑一次并逐字符检查产物；出现 `…`、占位符或空段残留，就是关闭不彻底 |

---

## C-012 拿本机绿测当跨平台验收

| 项 | 内容 |
|---|---|
| 发生 | 2026-09-12 |
| 场景 | `dsh-redaction` 仓库第一次跑 GitHub Actions CI（`ubuntu-latest`）。在此之前，全部开发与自测都在 Windows 上完成，两个包的 9 套套件本地全绿 |
| 我的错误结论 | 「本机 9 套全绿 ⇒ 交付物在任何平台都跑得起来；CI 只是把本机已经做过的事再做一遍」——于是 CI 一直没推上去，也没有人觉得这件事着急 |
| 事实 | CI **37 秒就红**。失败点为 `packages/dsh-plugin-redact/test/preflight.mjs`：`TypeError [ERR_INVALID_ARG_TYPE]: The "path" argument must be of type string. Received undefined`，位置在 `path.join`——**不是断言失败，是套件根本起不来**。根因是该文件用 **`process.env.USERPROFILE`** 推导 DSH 主目录；这个环境变量**只存在于 Windows**，在 `ubuntu-latest` 上是 `undefined`，于是 `path.join(undefined, '.dsh')` 直接抛错 |
| 为什么是**发布物缺陷**而不是 CI 配置问题 | `test/preflight.mjs` 在 `package.json` 的 `files` 白名单里，**会随包发布** ⇒ **任何 Linux / macOS 消费者跑 `npm test` 都会炸**。CI 只是第一个把它说出来的东西 |
| 同批另两处同类 | ① `new URL(import.meta.url).pathname`（2 个文件）：`.pathname` 是 **URL 组件**不是文件系统路径，目录含空格/中文时残留 `%20` 转义（Windows 上还会多出 `/C:/…` 前导斜杠），正确写法是 `fileURLToPath(import.meta.url)`；② 同一份 `engine.selftest.mjs` 在仓库与实际部署处**各有一份拷贝**，部署那份有一段夹具清理代码而仓库那份没有 → 每跑一次测试就在源码目录留下 `broken.zstd`、`needle.txt`、`plan-*.json`（同一文件两份拷贝悄悄分叉） |
| 错在哪一步 | 把「**在我这台机器上通过**」当成「在别人的机器上会通过」。**平台专有假设在写它的平台上隐形**——本地永远绿，因为本地从来就是它假设的那个平台。缺的那一步是**在目标平台上真的跑一次** |
| 可复用规则 | **平台假设必须由目标平台来验收。** 判据是「在目标平台上跑过」，不是「本机跑过」。跨平台交付物在推送/发布前，至少要在**一个目标平台**上执行一遍与 CI 相同的步骤 |
| 检测方法 | ① 搜平台专有符号：`USERPROFILE` / `HOMEDRIVE` / `APPDATA` / `LOCALAPPDATA`、`import.meta.url` 配 `.pathname`、字符串字面量里的盘符、`path.win32`；② 对每条命中问「它在目标平台上是什么值」——后面没有紧跟 `??` 或 `||` 回退的就是缺陷；③ 把这份检查固化成**静态守卫 + 自检**（见下） |
| 波及 | CI 首跑即暴露；修复后推送前在本地 1:1 复现 `ubuntu-latest` 全流程 **7/7 步全绿**，Windows 侧 9/9 套件全绿。缺陷**未流向任何消费者** |

> [!bug] 修法很浅，防线才是这条记录的价值
> 修复动作本身都不难：`os.homedir()` 取代平台专有环境变量、`fileURLToPath` 取代 `.pathname`、把夹具清理块补回仓库副本、`.gitignore` 兜底。真正让这条不再重犯的是新增的 **`.github/scripts/portability.mjs` 静态守卫**——扫两个包的**发布代码与测试**，四条规则：
> 1. 平台专有环境变量（`USERPROFILE` / `HOMEDRIVE` / `APPDATA` / `LOCALAPPDATA`）后面没有紧跟 `??` 或 `||` 回退；
> 2. `import.meta.url` 与 `.pathname` 出现在同一处；
> 3. 字符串字面量里硬编码盘符；
> 4. `path.win32`。

> [!warning] 守卫自己的两个假判据（比缺陷本身更值得记）
> 1. **行级判断 ≠ 变量级判断。** 第一版把「**这一行里**出现过 `??`」当作安全信号，于是 `DSH_HOME ?? path.join(process.env.USERPROFILE, '.dsh')` 被判为**安全**——而这**正是要抓的那个 bug**。正确判据是：回退必须属于**那个变量本身**（紧跟其后），而不是属于这一行。
> 2. **±2 行窗口会被自己的注释命中。** `import-meta-pathname` 规则最初用 ±2 行窗口匹配，结果被它**自己那句解释性注释**（注释里写了 `.pathname` 四个字）误报。改成只在**同一行、同一表达式内**匹配。
>
> 两条合起来是一条规则：**守卫的判据必须与缺陷同形**（变量级，而不是行级），**匹配范围必须窄到不会命中解释性文字**。

> [!important] 守卫必须可证伪
> 配套的 `portability.selftest.mjs` 会**把真实的 `USERPROFILE` 缺陷重新注入 `preflight.mjs`**，断言守卫必须（a）以**退出码 1** 失败、（b）**指名正确的文件与正确的行号**，然后用 `finally` 把原文件恢复。理由：**不会失败的检查比没有检查更糟**——它把「没查」伪装成「查过了」。
> 顺带一个细节：自检最初把期望行号写死成 `13`，后来在文件顶部加了一行 import 就红了；改成从注入后的内容里**动态推算**行号——判据不能依赖「文件此刻长什么样」。

---

## C-013 为讲清教训而把敏感字面量写进会公开的文档

| 项 | 内容 |
|---|---|
| 发生 | 2026-09-12（**同日复发四次**） |
| 场景 | 清理完密钥/口令后，撰写说明性文档（注释、合并记录、README、以及本条自身） |
| 我的错误 | 在文档里**逐字引用被清理的值**，使这份**会公开推送**的文档自己带上了它们 |
| 复发四次 | ① `router_ssh.sh` 注释里写口令字面量；② 合并记录里引用 9 处密钥前缀；③ 本库条目自身又写口令字面量；④ `README.md` 的脱敏章节写密钥前缀 |
| 事实 | 脱敏是针对「值」的，而**「值的部分」也是值**——前缀可与别处文档拼接还原 |
| 错在哪一步 | 把「文档」与「要脱敏的原始材料」当成两个世界；实际上**我写的文档也会进入公开仓库** |
| 可复用规则 | **写脱敏说明时，用类别描述代替任何字面前缀**：写「一个 4 位前缀」，不写实际前缀字符串。**教训本身不需要复现凭据才成立。** |
| 检测方法 | 完成任何记录/复盘/README 类文档后，对**该文档自身**跑一次与代码相同的敏感扫描 |
| 元教训 | **同一错误四次复发 ⇒ 这不是疏忽，是流程缺陷。** 人的注意力无法长期对抗这种错误，必须落成机器约束——已实现为 `scripts/prepush-selfscan.sh`（本地闸）与 `.github/workflows/security-scan.yml`（CI 二次拦截） |

> [!danger] 本条自身就是例证
> 第一次写这一条时，我在「复发四次」的说明里**又写了口令字面量**，被自己的扫描脚本抓出。
> **这恰好证明：靠"记住要小心"是无效的，必须有工具。**

---

## C-014 检查工具只加模式不加豁免，被误报淹没

| 项 | 内容 |
|---|---|
| 发生 | 2026-09-12 |
| 场景 | 按 C-013 的结论实现 `scripts/prepush-selfscan.sh` 后**首次运行** |
| 现象 | 脚本在 `README.md` 报 2 处、在合并记录报 1 处「命中」，**全部是文档示例**（截断密钥的占位写法、`-p` 加省略号的示意、讨论用户名路径的说明文字） |
| 若不理会的后果 | 每次扫描都有固定误报 → 使用者习惯性忽略输出 → **真凭据混在误报里被放过**。检查工具沦为形式 |
| 错在哪一步 | 只设计了「发现即阻断」，**没设计「如何表达合法的例外」** |
| 可复用规则 | **任何检查工具必须同时给出两样东西：检出模式 + 显式豁免机制。** 豁免要**可见、可审计、最小化**——不允许靠模糊正则去「猜」哪些是示例，否则真凭据也会被猜成示例而漏掉 |
| 我的处置 | ① 收窄模式：`-p` 后的中文省略号不再匹配（只接受字母数字类实参）；本机用户名只在**路径形态**下报警；② 引入 `scan-ignore` 行内注释作为显式豁免；③ 加 `--self-test` 自证脚本自身不含凭据 |
| 检测方法 | 工具上线前自问：「如果我是使用者，第一次跑就见到误报，我还会认真看第二次吗？」 |
| 元教训 | **门禁的价值等于其信噪比，而非覆盖度。** 覆盖再全，只要误报率高就会被绕过——这与 C-002「过度定级稀释可信度」是同一道理 |

> [!note] 与 C-003 / C-012 的对称性
> C-003 是「扫描器太瞎」（漏报），C-014 是「扫描器太吵」（误报）——**两者都会让检查失效，而失效方向相反**。
> C-012 的守卫也曾被**自己的解释性注释**误报（改用同一行同表达式匹配）。三次是同一类问题的不同面：
> **检查的判据必须与缺陷同形，匹配范围必须窄到不会命中说明性文字；同时必须留出显式豁免。**

---

## C-015 把过滤器筛空的零命中当成「不存在」

| 项 | 内容 |
|---|---|
| 发生 | 2026-09-13 |
| 场景 | 发布与分发调研的第一步：核实 DSH 有没有插件管理子命令 |
| 我的错误结论 | 「**DSH 没有 `plugin` 子命令**」——据此把"官方通道缺失"当成一个待解释的现象 |
| 事实 | `bin.js:105` 就写着 `dsh plugin`。根因是**一个写错的过滤器**：`-notmatch '\\node_modules\\'` 把 **8677 个文件全部排除**，**实际扫描 0 个文件**，却报告"未发现" |
| 错在哪一步 | 把「扫描结果为空」直接读成「目标不存在」。**过滤器把待查对象 100% 筛掉，返回的零命中被当成了结论**——与 C-003（扫描器是瞎的）同形，只是这次瞎的是自己的过滤条件 |
| 可复用规则 | **零命中要先怀疑过滤器本身。** 下"未发现"结论前，先证明扫描器看得见目标；并且**打印实际扫描的文件数**——0 就是过滤器故障，不是结论 |
| 检测方法 | 任何"未发现/不存在"结论，回读命令并回答："它扫了几个文件？"答不出或答案为 0，结论直接作废；再把过滤条件取反跑一次，看是否命中 |

> [!bug] 这条的形状很刺眼
> 排除 `node_modules` 是**完全正确**的意图，错的是它在目标目录里**把所有东西都排除了**（该目录整棵树都在 `node_modules` 下）。**正确的过滤条件 + 错误的适用范围 = 零命中**，而零命中被读成了事实。

---

## C-016 凭推断给扫描器定行为，方向反了

| 项 | 内容 |
|---|---|
| 发生 | 2026-09-13 |
| 场景 | 为让插件被目录收录，权衡是否在 monorepo 根加 `package.json` |
| 我的错误结论 | 「monorepo 里加根 `package.json` **可能误导扫描器**」——据此把它当成风险项 |
| 事实 | **方向完全相反**：根 `package.json` 的 `dsh.bundles` **正是 catalogue 发现子包的机制**；仓库根没有 `package.json` ⇒ 数组为空 ⇒ 子包对扫描器**等于不存在**，topic 加得再对也没用 |
| 错在哪一步 | 用「根上多一个 manifest 会干扰工具」的**直觉**替代了"去读那个扫描器的实现"。整个判断从头到尾没有一次观测 |
| 可复用规则 | **对"某工具会怎么处理某个文件"的判断，必须读该工具的实现或做一次真实观测。** 推断不能当证据——它连方向都可能是反的 |
| 检测方法 | 把判断改写成可执行的对照实验："加 / 不加根 `package.json`，各扫一次，看子包是否被发现"；跑不出对照实验的判断不写进结论 |

---

## C-017 把「写法不同」当成「不一致」

| 项 | 内容 |
|---|---|
| 发生 | 2026-09-13 |
| 场景 | 复盘一处路径推导实现时，指认某函数"与库内其它地方不一致" |
| 我的错误结论 | 「`otherCopies()` 用 homedir 找 `.dsh-tui` 是**不一致**」——报为一个潜在问题 |
| 事实 | **核实后是对的**：dsh-tui 自己就用 `join(homedir(), '.dsh-tui', …)`（`bin/dsh-tui.js:358`、`:569`）。被指认方与本项目**行为一致**，不存在不一致 |
| 错在哪一步 | 拿"我以为的约定"当基准，**没有去读被指认方的源码**就报缺陷。这是 C-002（过度定级）与 C-014（误报）的同族：**误报会稀释真问题的可信度** |
| 可复用规则 | **指认"不一致"之前必须读双方源码。** "看起来不同"不是证据；能把两个 `文件:行号` 写出来，才叫结论 |
| 检测方法 | 每条"不一致/不一致风险"结论后追问："被指认方自己是怎么写的？"写不出它的 `文件:行号`，就把这条降级为待查而不是缺陷 |

> [!note] 撤回比错误本身更有价值
> 这条的价值不在"我少报了一个 bug"，而在**它标出了一类假阳性**：凡是"和我印象中的约定不符"的判断，默认是**我印象错了**。

---

## C-018 把「应该有」当成「已有」

| 项 | 内容 |
|---|---|
| 发生 | 2026-09-13 |
| 场景 | 供应链核查：判断官方 profile 模板是否默认带 pnpm 的供应链延迟保护 |
| 我的错误结论 | 「官方 profile 模板带 `minimumReleaseAge` 供应链保护」 |
| 事实 | 实测该值为 **`undefined`——未启用**；配套的 `minimumReleaseAgeExclude` 列表因此是**空转的**（列了也没有东西可排除） |
| 错在哪一步 | 把"这类项目通常会配这个保护"当成事实，**没有去读实际配置值**。这是 C-001 的形状：**应该有 ≠ 实际有** |
| 可复用规则 | **任何"有保护 / 已启用 / 已配置"的断言，判据是实际值。** 供应链结论尤其不能靠惯例推断——它直接决定"要不要自己补一道" |
| 检测方法 | 断言后面附一条能打印实际值的命令与它的真实输出；输出为 `undefined` / 空 / 默认值时，结论必须写成"未启用" |

---

## C-019 把「表单存在」当成「提交通道可用」

| 项 | 内容 |
|---|---|
| 发生 | 2026-09-13 |
| 场景 | 为插件找手动收录入口，看到某目录站点有网页提交表单 |
| 我的错误结论 | 「该站点的提交表单可用」——据此把它列为可用的提交通道 |
| 事实 | 表单长期返回 `The submission service is not configured yet`（**服务端未配置**）；该仓库的 tracker 里已有 issue 标题直接写着 `(web submission endpoint unavailable)` ⇒ 只能走 GitHub issue |
| 错在哪一步 | 看到"页面上有这个入口"就当成"通道可用"，**没有做一次真实提交，也没有去看现有记录**。这是 C-009「服务存在 ≠ 效果可达」在外部服务上的同形 |
| 可复用规则 | **入口存在 ≠ 服务可用。** 提交通道的判据是**有一次成功提交**（自己成功，或读该通道最近的成功记录），不是"页面上有这个按钮" |
| 检测方法 | 用一次真实提交（成本最低的形态）验证；或读该通道最近的成功记录。两者都拿不到，就把这条通道标为"不可用"并换路 |

---

## C-020 把「命令退出 0」当成「副作用已生效」

| 项 | 内容 |
|---|---|
| 发生 | 2026-09-13 |
| 场景 | 用 `gh issue create --label plugin-submission` 向一个第三方插件目录仓库提交条目（该仓库的 `plugin-submission.yml` 模板里声明了 `labels: ["plugin-submission"]`） |
| 我的错误结论 | 「命令退出码 0 ⇒ 提交已按预期带上 `plugin-submission` 标签」——据此认为提交完成 |
| 事实 | **标签为空。** 事后 `gh issue edit --add-label` 才报出真正原因：`GraphQL: ... does not have the correct permissions to execute AddLabelsToLabelable`——**非维护者无权打标签**，创建时那个 `--label` 因此被**静默丢弃**，而命令仍然退出 0 |
| 错在哪一步 | 把「命令成功」当成「副作用落地」。退出码只证明**请求被接受**，不证明**字段被写入**；中间缺的那一步是**一次独立的读操作** |
| 可复用规则 | **创建类操作必须以一次独立的读操作收尾。** 创建后回读那条记录，把关键字段（标签、状态、可见性、关联、目标仓库）与预期逐项对照。凡是"工具会自动带上"的东西，都要回查一次。**但回查发现的异常不等于失败原因**（见下方撤回说明） |
| 检测方法 | 每个创建动作后立刻跑一次读命令（如 `gh issue view --json labels`），把实际值与预期逐字段比对；对不上就当场补或换方式，不要等到别人来发现 |
| 波及 | 该仓库其它所有提交都带 `plugin-submission`（通过预检的还带 `submission-passed`），**只有我们这条什么都没有**——我据此推断"它的自动化靠标签触发"。⚠️ **该推断已被同日推翻**：其 workflow 触发条件是 **OR**（`contains(github.event.issue.labels.*.name, 'plugin-submission')` **或** 标题以 `[Plugin]` 开头），我们的标题满足第二分支，**workflow 在创建后 8 秒就跑了**——标签从来不是必需的。"缺标签导致不被处理"是错的，已在 issue 上评论撤回 |

> [!bug] 本条有一半已被同日撤回（2026-09-13）
> **成立的部分**：无权打标签时 `gh issue create --label X` **确实静默失败**（退出码 0、标签为空；`gh issue edit --add-label` 报 `does not have the correct permissions to execute AddLabelsToLabelable`）。这个**机制**为真，"创建后用独立读操作回查"这条规则继续有效。
> **被撤回的部分**：由"标签为空"推出"所以这条提交没被处理"。**机制为真，因果为假。**
> 提交失败的真实原因另有所在，见 [[#C-021 把 issue 表单当成给人看的渲染层]] 与 [[#C-022 把「人知道它不可用」当成「工具知道」]]。
> **这一轮本身又贡献了一条规则**：回查到的异常是**线索**，不是**结论**——把它当结论，就是 C-014/C-017 的同族（拿一个看起来合理的解释取代排查）。

> [!important] 与第四节守卫的同一条思路
> 「守卫必须可证伪」拒绝的是"检查看起来通过了"；本条拒绝的是"操作看起来成功了"。
> 两者都是同一句话：**不接受"看起来成功了"，只接受一次落在对方系统里的、可回读的证据。**
> 发生现场见 [[DSH插件发布与分发]] §6.3。

---

## C-021 把 issue 表单当成给人看的渲染层

| 项 | 内容 |
|---|---|
| 发生 | 2026-09-13 |
| 场景 | 用 API/CLI（`gh issue create`）向一个使用 **issue 表单**（`.github/ISSUE_TEMPLATE/*.yml`）的第三方目录仓库提交插件 |
| 我的错误结论 | 「表单只是给人填的界面；我写一份信息量足够的 markdown 正文，效果一样」 |
| 事实 | 该仓库的自动化读的是 **issue 表单的字段结构**：用正则 `/^### (.+)$/` 切分 `### <字段名>` 段落，再按 `GitHub repository` / `Plugin path (optional)` 这样的标签取值。**字段名对不上就报 `SUBMISSION_FIELD_MISSING`**——正文写得再全也不算 |
| 错在哪一步 | 把表单当成**渲染层**，没想到它是**数据结构契约**。用 API 提交等于绕过了表单的序列化过程，却没有自己补上它产出的结构 |
| 可复用规则 | **issue 表单是自动化接口的一部分。** 用 API/CLI 代替表单提交时，必须**复刻表单会生成的那套 `### 字段名` 布局**，字段名逐字照抄（含 `(optional)` 之类的后缀） |
| 检测方法 | 提交前把表单 yml 的字段名列表抄出来，逐条确认正文里有对应的 `### 字段名` 段落；提交后读**对方预检的结论**，而不是读自己的正文 |

> [!note] 判据仍在对方那一侧
> 这条与它的邻居们同源：正文"看起来完整"是我这一侧的性质；**能不能被解析**是对方那一侧的性质。这也是 [[DSH会话脱敏项目方法论复盘]] §1.4 的同一句话。

---

## C-022 把「人知道它不可用」当成「工具知道」

| 项 | 内容 |
|---|---|
| 发生 | 2026-09-13 |
| 场景 | 同上：第三方目录仓库的预检遍历仓库树收集候选插件 |
| 我的错误结论 | 「那几份快照放在 `docs/` 下，而且在 `docs/reports/boot-verify/README.md` 里已标注为不可用，不会干扰任何工具」 |
| 事实 | 预检把**所有带 `dsh.bundle` 的 `package.json`** 都当成候选插件。仓库里除两个真实包外，`docs/reports/boot-verify/{a,b,frozen}/` 下的 5 份历史快照与 `docs/design-notes/bakeoff-policy-engine/` 的设计原型——**因为是从早期版本逐字节冻结的副本，仍然带着 `dsh.bundle.patch` 字段**——于是被识别成 **7 个候选插件**（真实只有 2 个），报 `SUBMISSION_PACKAGE_AMBIGUOUS` |
| 错在哪一步 | 把"**人**知道它不可用"当成"**工具**知道它不可用"。**README 不是机器可读的排除条件**；工具的判据是**字段存在性**，不是文档说明 |
| 可复用规则 | **仓库里任何"完整可安装形态"的副本都会被工具当成真货。** 冻结 / 归档副本必须在**结构上**退出识别面（改名字段、去掉 manifest 键），而不是靠文档标注 |
| 检测方法 | 对每个 `docs/` 下的快照问一句"它现在还能被安装吗"——能，它就会被任何遍历目录树的扫描器算进去。用一条 `grep -rl '"dsh"' --include=package.json` 数一遍候选数，与真实包数对照 |
| 处置 | 提交时显式填 `Plugin path`；根治是把快照里的 `package.json` 改名（如 `package.json.frozen`）或去掉 `dsh.bundle` 字段 |

> [!important] 这条把"卫生问题"升级成了"功能问题"
> 快照留在仓库里，以前只被当成"目录不够干净"；**现在它直接导致第三方收录失败**。
> 与 C-003（扫描器是瞎的）方向相反、结论相同：**工具的可见性与人的可见性不是一回事**——要么两边都干净，要么别让副本保持可识别形态。

---

## C-023 拿一个代理指标外推出机制，并当作结论说出

| 项 | 内容 |
|---|---|
| 发生 | 2026-09-13 |
| 场景 | 长会话中调查缓存命中率、DSH 性能、模型窗口三件事 |
| 我的错误结论 | **同一形态出现六次**：①`0.82% 单核/runner` ⇒「孤儿 runner 就是 80–138% 的成因」；②dump「152 次锚点指纹跳变」⇒「半数请求会改缓存前缀」；③整包**字节**前缀 ⇒「生长中的对话永远不是纯追加」；④命令行被截断的 `--profil…` ⇒「找到了，是开着性能剖析」；⑤transcript 里 **95 次** `<relay:relocated-context>` ⇒「搬迁内容被回写、逐轮膨胀」；⑥`9.6M/51 ≈ 189K/次` ⇒「更像空闲过期，故修窗口未必提升命中率」 |
| 事实 | 六条**全部被推翻**：①需 200–345 个 runner，实测 **1 个**；②实为 100+ 并发 agent **时间交错**的假象，按真实世系分组后**组内 0 次跳变**；③缓存是 **token 前缀**（system→tools→消息序），不是 HTTP 字节前缀，实测命中率 96.7% 当场证伪；④`--profile <name>` 是**配置档选择器**，`~/.dsh/profiles/dsh-tui/` 实证，且全机无 `.cpuprofile`；⑤**96/102 条是我自己引述该标记留下的**，真正紧跟系统提示正文的只有 6 条；⑥实测 42 次请求 `cache_read` 单调增长一次未掉，空闲 **168.9s 命中率反而 99.0%** |
| 错在哪一步 | **拿到一个代理量就补全成「能解释观测的机制」，没问「这个数在量什么」「是谁产生的」。** 没有一次是数据错，全是**口径**错 |
| 可复用规则 | **代理量 ≠ 目标量。** 说结论前先过两问：**「它在量什么」**（与目标量是否同一回事）、**「它是谁产生的」**（尤其当搜索目标是我正在写入的文件、或统计里会混入我自己的行为时）。另：**给结论标证据等级**——实测 / 厂商原文 / 推断；**推断不要写成结论** |
| 检测方法 | ①在文件里 `grep` 某字符串前，先排除**自己写进去**的那些（否则搜到的是自己的回声）；②比对「我改的变量」与「我观测的量」是否同一维；③优先**受控 A/B**（只改一个变量）而非观察性数据；④优先**被测系统自己的话**（厂商 stderr 原文、二进制函数体、官方文档） |
| 处置 | 三次已向用户撤回结论；本条为归纳。与 C-009 / C-016 同族，同属「把推断或一端的结果当成证据」（见总纲第四个变体） |

---

## C-024 拿静态审查的绿灯当「真实流量下无缺陷」的证据

| 项 | 内容 |
|---|---|
| 发生 | 2026-09-13 |
| 场景 | cache-relay 诊断探针（P6）上线 |
| 我的错误结论 | 探针补丁已过 `node --check`、单测 16 项全绿、并经 **32-agent 对抗审查**，可以放心上线 |
| 事实 | **上线后立刻暴露 3 个缺陷，静态审查与单测全部漏掉**：①分类器响应**不是 SSE**（643B～3KB 整段 JSON，无 `data:` 行）⇒ usage 恒为空，而那是该探针唯一用途；②`cache_control` 断点位移致**每请求假阳性**（实测 10/10 全假，而同期 `cache_read` 单调增长证明前缀其实稳定）；③「末条消息变化」是**稳态尾部翻动**而非前缀重置（`firstDiffIdx ≡ prevMsgs-1`） |
| 错在哪一步 | 把「**静态**审查通过」当成了「**真实流量**下无缺陷」。三者都是**真实流量的形态**：非 SSE 响应、断点位移、末条翻动——单测的 fixture 里根本不存在这些形状 |
| 可复用规则 | **诊断工具自身也需要「上真流量再验一遍」这道工序。** 上线后**立刻核对它的产出是否合理**——本次是靠「每请求都落一条」这个**不合理现象**才发现假阳性的。产品代码如此，**观测代码尤其如此**：观测器坏了不会报错，只会安静地给出错的数 |
| 检测方法 | 上线后第一件事不是看结论，而是**看产出的形状**：频率是否合理、字段是否缺失、分布是否退化。任何「每条都触发」「全为 0」「全为空」都是**仪器可疑**的第一信号 |
| 处置 | 三个缺陷均已修复并各配回归测试（`relay-probe-test.mjs`）。与 C-007（拿配置组合当运行时证明）、C-008（拿绿色测试当可用性证据）同族 |

---

## C-025 自己留下的失控进程污染了自己的全部测量

| 项 | 内容 |
|---|---|
| 发生 | 2026-09-13 |
| 场景 | 对 DSH harness 做 CPU 采样、判断「空转 vs 真负载」 |
| 我的错误结论 | （**未及时意识到**）我一直在做性能结论，却不知道机器上有**我自己**留下的两个失控进程在烧 |
| 事实 | 两个 Bash **递归 `grep`** 的父 shell 早已退出，`grep.exe` 却成为孤儿继续跑：其一在**整个用户主目录** `<用户主目录>` 上扫了近 **2 小时**，吃掉 **4714 CPU 秒 + 534MB**；另一个 752 CPU 秒。合计约 **0.7 个核持续两小时**，外加**对整个主目录的海量磁盘 I/O** |
| 错在哪一步 | **没有在做性能结论前先做进程普查。** 而且这类孤儿既是真实的机器负担（用户当场就报「卡顿」），**又会污染同期的每一个测量**——我在同一窗口里对 harness 做的采样、以及由此得出的「外因 vs 内因」判断，全建立在这个背景噪声之上 |
| 可复用规则 | ①在 Windows 上跑**递归** `grep`/`find` 时**范围必须收窄**，绝不要对 `C:/Users/<user>` 这种整个主目录下手；长扫描用后台运行以便收尸。②**下性能结论前先进程普查**。③判别「内因 vs 外因」的标准做法：**先清除外因，再复测**——本次清掉 grep 后 harness 仍 136.3%（原 138.8%），才敢下「是它自己空转」的结论 |
| 检测方法 | `powershell -NoProfile -Command "Get-Process grep,find,rg -EA SilentlyContinue \| Select Id,CPU,StartTime"` —— 看有无 CPU 秒数异常（数百至数千秒）且父进程已消失的进程 |
| 处置 | 两个孤儿已 `Stop-Process -Force` 清除（它们是我自己拉起的，不是用户的进程） |

---

## C-026 拿单次测量当可复现结论

| 项 | 内容 |
|---|---|
| 发生 | 2026-09-19 |
| 场景 | 测 `granite4:tiny-h`（7B-A1B MoE）在 4GB 卡上三种 GPU 配置的解码速度 |
| 我的错误结论 | 首轮测出「默认分层 **36.0 tok/s**」，并写出「**默认分层反而是最快**」——与「强制满 GPU」测得的 18.6 tok/s 形成对照，听着像个反直觉发现 |
| 事实 | 复测两轮：默认分层 **18.7 / 18.5**、纯 CPU **17.1 / 17.2**、强制满 GPU **18.6 / 18.4** —— **三种配置实际几乎同速（17–19），36.0 是孤立假值** |
| 错在哪一步 | 只跑了一次就把数字当成可复现结论，且**在没有重复的情况下给出了机制解释**（"分层反而最优"）。单点样本 + 编机制 = 双错 |
| 可复用规则 | **任何要写进交付物的性能数字，至少跑两轮。** 两轮不一致时先怀疑测量环境（模型是否已驻留、有无并发负载、是否命中缓存），**不要先编机制**——机制会让假数字显得可信 |
| 检测方法 | 同一配置连跑两次并比对；差异 >10% 即判该数字无效，必须查到原因才能引用 |
| 处置 | 已在下一轮复测后向用户更正。与 [[#C-023 拿一个代理指标外推出机制，并当作结论说出]] 同族（都是"单点观察 → 补全成结论"） |
| 现场 | 知识库 [[本地LLM部署与显存实测-4GB卡]] 第八节 |

---

## C-027 把自己工具的缺陷当成被测对象的缺陷

| 项 | 内容 |
|---|---|
| 发生 | 2026-09-19 |
| 场景 | 用 bash + curl 向本地 Ollama 发中文提问 |
| 我的错误结论 | 模型的思考链里写着"这段中文看起来是乱码"，我据此判定「**中文到达模型时已损坏**」，并**归因到 GPU 内核**——"正好命中此前分析标记的头号风险：Gated DeltaNet 在 Turing 上的兼容性" |
| 事实 | 换 Python 显式 UTF-8 编码发同一问题，模型**正确读出** `1+1 等于几？`（字节 dump 为合法 UTF-8）。**乱码源自我自己用 bash+curl 传非 ASCII 的编码问题**，与 Ollama、与 GPU 架构都无关 |
| 错在哪一步 | 观察到异常后，**先去找了最戏剧化的解释（GPU kernel bug），而没有先逐环排查自己的链路**（shell 编码 → curl → HTTP body → 服务端） |
| 可复用规则 | **异常出现时，先怀疑自己的工具链，再怀疑被测对象。** 判据：**能否换一条独立路径复现同一现象**——换路径就消失，就是自己的问题。（同 C-003「扫描器可能是瞎的」，只是这次瞎的是我的发送端） |
| 检测方法 | 交叉路径复现：同一输入换一种发送方式（bash/curl → Python/requests）重跑，结果不同即锁定为工具链缺陷 |
| 加重情节 | 该错误结论**恰好与一个此前未验证的假设吻合**。这种"正好印证"的契合感会显著降低核查意愿——**越是对上了，越要独立验证** |
| 现场 | 知识库 [[本地LLM部署与显存实测-4GB卡]] 第八节 |

---

## C-028 基准测量被服务端缓存污染（防缓存手段本身没验证）

| 项 | 内容 |
|---|---|
| 发生 | 2026-09-19 |
| 场景 | 测 Ollama 的长提示词 prefill 速度（5.7K token） |
| 我的错误结论 | 首轮测出 **831.8 tok/s**；意识到可能命中缓存后加随机 nonce，改后测出 **4181.9 tok/s**，一度以为"数字更干净了" |
| 事实 | 我**把 nonce 加在了提示词末尾**。Ollama 按**最长公共前缀**缓存 KV，前缀照旧命中，只有末尾几个 token 需要真算 ⇒ 测出 **419 万 tok/s** 的荒谬值。nonce 改到**开头**后才得真实值 **915.9 tok/s** |
| 错在哪一步 | 知道要做 cache-busting，但**没有验证 cache-busting 本身是否生效**——加了 nonce 就当缓存已排除，没有用"这个数量级物理上可能吗"反查一次 |
| 可复用规则 | **加了防缓存/防污染手段后，必须反验它生效了。** 最简单有效的反验是**量纲校验**：用硬件上限卡一次——本机 GDDR5 约 128 GB/s，5.7K token 的 prefill 若报出四位数 tok/s，那不是好消息，是测量无效的信号 |
| 检测方法 | 用物理上限做 sanity check：`权重体积 ÷ 显存带宽` = decode 理论上限；任何实测值**超过**该上限即证明测量被污染，直接作废 |
| 处置 | nonce 改前置后重测，prefill 数字已全部更新；脚本内已加注释说明"必须加在开头" |
| 现场 | 知识库 [[本地LLM部署与显存实测-4GB卡]] 第八节；修复后的脚本 `D:\OllamaModels\bench\bench_model.py` |

---

## C-029 对一个故障连续三次把「能解释症状的单一现象」当成根因

| 项 | 内容 |
|---|---|
| 发生 | 2026-09-19 |
| 场景 | 把本机 Ollama 接进 DSH（DeepSeek Harness）当 agent 后端，**每一轮都报 `turn max-tokens`**（`content` 为空、`outputTokens: 1`） |
| 我的错误结论 | 连续三次误判：①「3B 级模型产不出合法 tool_call」⇒ 建议换 gpt-oss:20b；②「录包见 `max_completion_tokens: 1`」⇒ 认定就是这个字段；③「`maxTokens` 被 34 个工具的开销吃光」⇒ 把 `maxTokens` 从 4096 提到 12288 |
| 事实 | 真因是 **`contextWindow`(16384) 小于 DSH 的提示词估算**——它的估算**包含 34 个工具的 JSON schema（38,653 字符 ≈ 16.6k token）**，剩余预算 = 16384 − 16559 = **负数 ⇒ 钳到 1**。把 `contextWindow` 与 Ollama 同步提到 **32768** 后，录包实测 `max_completion_tokens` **1 → 12288**，端到端跑通。三个误判各自被推翻：① 空函数名只是「预算=1 个 token」的**另一个症状**（窗口修好后同一模型产出 `name="ask_user_question"`、参数可解析）；② **Ollama 完全忽略 `max_completion_tokens`**（发 1 得 38 token、发 5 得 48 token），它不可能是成因；③ 改 `maxTokens` 后该值**仍是 1** |
| 错在哪一步 | **每次都"抓到一个能解释症状的现象，就停止排查"。** 既没做「**只改一个变量**看它变不变」的对照，也没在本地把因果链完整复现。更糟的是三次改动**叠加在一起**（`reasoningEfforts` + `maxTokens` + `contextWindow` 同时改），导致即便改对了**也无法事后归因是哪个生效** |
| 可复用规则 | ① **诊断时一次只改一个变量**，改完必须复测确认这个变量**真的影响了目标值**；② **"能解释症状" ≠ "是原因"**——判据是能预测「改掉它，症状消失，且只消失这一个」；③ **多个假设同时验证 = 事后无法归因**，等于白做 |
| 检测方法 | 改配置**前**写下「我预期这个改动会让 X 从 A 变成 B」；改完**只测 X**。若 X 没变，**立刻撤回这个改动**再试下一个，绝不叠加 |
| 突破口 | 真正让答案浮出来的是**对比两次 `n_tools` 不同的录包**（0 个工具 ⇒ 4096；34 个工具 ⇒ 1）。差异一出现，相关变量立刻现形——**做对照比做推理快** |
| 元教训 | 三次误判**都很有说服力**：每个现象都真实存在、每条推理链都自洽。**自洽 ≠ 正确**——这正对应 [[#C-023 拿一个代理指标外推出机制，并当作结论说出]]，只是这次的对象是**我自己的排查过程** |
| 现场 | 知识库 [[本地LLM部署与显存实测-4GB卡]] §六（含完整证据表与对比数据） |

---

## C-030 拿被自己污染的实验得出的负结果当结论——并已写入交付物

| 项 | 内容 |
|---|---|
| 发生 | 2026-09-22 |
| 场景 | 验证 `LLAMA_ARG_FIT_TARGET=256`（降低 llama.cpp 保守余量、让更多层上 GPU）是否提速 |
| 我的错误结论 | 「**负收益**：decode 从 26.2 掉到 **11.7 tok/s**」，并归因为「显存压到 92% 触发共享内存溢出」。**该结论已写进知识库文档并推送到公开远端** |
| 事实 | 干净复测：默认 **36.4 tok/s @51% GPU** vs `FIT_TARGET=256` **34.9/35.4 tok/s @68% GPU** ⇒ **中性（−3%，噪声内）**，且它确实按设计把 GPU 占比提到 68%。真因是**我的孤儿进程**：重启 Ollama 时 `taskkill ollama.exe` 未杀掉子进程 `llama-server.exe`，孤儿占着显存 ⇒ Ollama 只看到 **1479 MiB 可用**（正常 2910–3238）⇒ 只能塞 **6/41 层**（正常 41/41）⇒ 慢 |
| 错在哪一步 | **下结论前没做进程普查，也没核对实验装置的等价性。** 日志里 `free=1479` 与正常的 `free=2910~3238` 已经明明白白不一致——**只要看一眼日志就能发现两次实验的起点根本不同**，而我直接对比了 decode |
| 可复用规则 | ① 做 A/B 前**先验证两次实验的装置等价**（本例：`free=` 是否一致）。**装置不等价，结论一律作废。** ② **负结果必须追根因再下结论**——"方案正确却负收益"时，先怀疑**实验装置**，不要先怀疑方案（用户明确要求过这条）。③ **单侧异常不要归因给被测对象**，先查自己留下的东西 |
| 检测方法 | 实验前后各跑一次 `Get-Process ollama,llama-server`（必须为零才开测）；从 `%USERPROFILE%\AppData\Local\Ollama\server*.log` 读 `memory breakdown` 的 `free=` 与 `offloaded N/M layers`，**确认两次一致** |
| 加重情节 | 这是 [[#C-025 自己留下的失控进程污染了自己的全部测量]] 的**第三次复现**（第一次递归 grep、第二次 llama-server 孤儿）。前两次污染的只是**数据**，这次污染**产出了一个错误结论**，且已外发 |
| 现场 | 知识库 [[本地模型能力矩阵与任务路由]] §四·六（含完整更正过程）；[[本地模型能力矩阵与任务路由]] §四·六（llama-server 孤儿） |

---

## C-031 把两件不相干的事捆进同一条「负对照」，导致归因完全反转

| 项 | 内容 |
|---|---|
| 发生 | 2026-09-22 |
| 场景 | 测本地小模型驱动 LangGraph 的能力，用「1+1 不该调工具」作为负对照 |
| 我的错误结论 | 「`granite4:micro-h` 负对照挂（答成 3）、`qwen3.5:0.8b` 过」——**两个方向的归因都反了** |
| 事实 | 单独裸问 5 次（temperature=0）：`granite4` 稳定答 **2**、`qwen3.5:0.8b` 稳定答 **3**。与图测试的结论**正好相反** |
| 错在哪一步 | 我把「**有没有乱调工具**」和「**算术算得对不对**」捆在同一条判据里。前者是这条负对照真正要测的东西；后者是**另一种能力**，且会随上下文（system prompt / 是否绑 tools）翻转。捆在一起后，翻转的那个信号把稳定的那个信号盖掉了 |
| 可复用规则 | ① **一条判据只测一件事**。想把"顺带看看"的信号塞进同一条断言时，先问：这两个量的**稳定性来源**是一样的吗？② 关键结论**必须换一条独立路径复现**（图里测完，再到裸 API 上单独测一次） |
| 检测方法 | 判据拆分后重跑：`negative 不误调`（只判工具调用）与 `arith`（只判算术，标注"仅参考不计入"） |
| 现场 | [[本地模型能力矩阵与任务路由]] §四·九·C |

---

## C-032 凭本地工作副本断定「远端未修复」——实际远端是修好的

| 项 | 内容 |
|---|---|
| 发生 | 2026-09-22 |
| 场景 | 推送前脱敏扫查，发现 `scripts/ollama-local/test_longrun.py` 里仍是用户名路径，而我此前已就该问题提交过修复 |
| 我的错误结论 | 对用户说：「**之前那次『已修复并推送』并没有真正生效**」 |
| 事实 | 提交 `5c89e22` 真实存在，`origin/main` 上的版本**已经是修复版**（`os.path.expandvars(r"%LOCALAPPDATA%" ...)`）。**有问题的是本地那份旧副本，且它处于 untracked 状态** |
| 错在哪一步 | 我在本地磁盘上看到旧内容，就直接推断"远端也是旧的"。**没有先查 git**。本地与远端分叉时，本地磁盘内容**不能代表远端** |
| 可复用规则 | ① 判断"远端有没有某内容"，**只认 `git show origin/main:<path>`**，不看本地工作区。② untracked 的旧副本是**推送时的定时炸弹**——它不在历史里，容易被当成"已处理过"而放行 |
| 加重情节 | 这条错得**方向相反**：我把"其实做对了的事"报成了"没做"。与 C-001~C-004（把外部信息当结论）同源，只是这次误判的是**自己的历史** |
| 现场 | 该文件已在推送前修正为与远端一致；本次推送的候选清单已按 `git ls-tree origin/main` 逐项核对 |

---

## 录入协议

新增条目时：

1. **编号**：`C-00N`，递增，**不复用已撤回条目的编号**
2. **必填六项**：发生时间 / 场景 / 我的错误结论 / 事实 / 错在哪一步 / 可复用规则
3. **必须写「检测方法」**——没有可执行的检出手段，这条记忆就无法在下次生效
4. **同步更新速查索引**（表格首行），否则等于没录
5. **不改写已录条目**——若某条后来被推翻，新增一条并回链，而不是修改原文（保留「我错过」这个事实本身）
6. **撤回的结论也要录**（如 [[#C-002 把可达性外推为可被利用]]）——**撤回比错误本身更有价值**，因为它标出了哪类推理会产出假阳性

> [!warning] 本库自身的失败模式
> 错误记忆库最危险的失败不是记错，而是**记了却没人查**。
> 因此：任何结论在被写入交付物前，应回查本库速查索引一次；发现命中即按对应规则重检。
> 该触发规则已写入 [[AGENTS]]，使新会话的 Agent 能载入本库。

---

## 关联

| 关联对象 | 关系 |
|---|---|
| [[AGENTS]] | 载入协议：约定 Agent 在形成结论前应回查本库 |
| [[MEMORY-INDEX]] | 姊妹库——那边记**架构模式**，本库记**判断错误** |
| [[subagent-lessons-learned-2026-07-03]] | 更早的子代理教训记录（本库的前身之一） |
| [[URL-REGISTRY]] | 错误 C-001/C-004 涉及的外部来源已登记其中 |
| [[DSH会话脱敏插件缺陷档案]] | C-005~C-012 的发生现场——插件缺陷档案（D-20~D-22 即本条 C-012），与本库互为回链 |
| [[DSH会话脱敏项目方法论复盘]] | C-005~C-012 的项目级复盘：同一批工作的判定链条总结（§六·五 是 C-012 的展开） |
| `%USERPROFILE%\dsh-redaction\.github\scripts\portability.mjs` | C-012 的防线：静态可移植性守卫；同目录 `portability.selftest.mjs` 用「把缺陷注入回去」证明它抓得住 |
| `%USERPROFILE%\dsh-vulnerability-analysis.md` | C-001 / C-002 的原始发生现场 |
| `%USERPROFILE%\dsh-dep-vuln-audit.md` | C-003（junction 盲区）/ C-004 的发生现场 |
| [[repo-merge-2026-09-12]] | C-013 / C-014 的发生现场与处置记录 |
| [[DSH插件发布与分发]] | C-015~C-022 的发生现场与处置记录（发布 / 分发 / 收录 / 供应链核查全流程） |
| `%USERPROFILE%\dsh-redaction\.github\scripts\pack-check.mjs` | C-016 的正面实现：monorepo 发现（根 `dsh.bundles`）与 manifest 闸门由它断言守住 |
| `scripts/prepush-selfscan.sh` | C-013 / C-014 的机器约束实现（本地闸） |
| `.github/workflows/security-scan.yml` | C-013 的 CI 二次拦截 |

## 变更记录

| 日期 | 变更 |
|---|---|
| 2026-09-12 | 建库；录入 C-001~C-004；建立速查索引与录入协议；提炼共同根源「把现象当结论」 |
| 2026-09-12 | 录入 C-005~C-011（DSH 会话脱敏项目：删行安全 / 渲染假设 / dump-config / 绿色测试 / 服务与效果 / disabled getter / 预算 0）；速查索引补 7 行；总纲按 11 条改写为两个变体；新增两条关联笔记链接 |
| 2026-09-12 | 录入 C-012（`dsh-redaction` 仓库首次推送 CI **37 秒红**：随包发布的测试用 Windows 专有的 `USERPROFILE` 推导 DSH 主目录；含 `import.meta.url` 配 `.pathname`、同名两份拷贝分叉）；速查索引补 1 行；总纲 11 → **12 条**；新增可移植性守卫的关联路径 |
| 2026-09-12 | 录入 C-013（敏感字面量写进会公开的文档，**同日复发四次**）、C-014（检查工具缺豁免机制被误报淹没）；速查索引补 2 行；总纲 12 → **14 条**并补第三个变体；修正此前误占用 C-005 编号的条目；落实机器约束 `scripts/prepush-selfscan.sh` + CI 工作流 |
| 2026-09-13 | 录入 C-015~C-020（DSH 插件发布与分发实践：过滤器筛空当「不存在」/ 凭推断给扫描器定行为 / 写法不同当不一致 / 应该有当已有 / 表单在当通道可用 / 退出码 0 当副作用生效）；速查索引补 6 行；总纲 14 → **20 条**并补第四个变体「把推断或一端的结果当成证据」；新增 [[DSH插件发布与分发]] 与 `pack-check.mjs` 两条关联 |
| 2026-09-13 | 录入 C-023（拿一个代理指标外推出机制——**同一形态六次实例、三次被迫向用户撤回**：`0.82%/runner`、`152 次跳变`、字节前缀、`--profile`、transcript 95 次命中、`189K/次`）、C-024（拿静态审查绿灯当「真实流量下无缺陷」——探针上线后暴露 3 个缺陷，单测与 32-agent 对抗审查全漏）、C-025（**自己留下的失控 grep 孤儿**跑 2 小时 / 4714 CPU 秒 / 主目录海量 I/O，污染全部性能测量）；速查索引补 3 行；总纲 22 → **25 条**；新增 [[claude-context-window-and-model-id]]、[[2026-09-12-flash-migration-context-shrink-postmortem]]、[[dsh-tui-cpu-spin-postmortem-2026-09-13]] 三条关联 |
| 2026-09-19 | 录入 **C-026~C-028**（本地 LLM 4GB 显存实测：单次测量当可复现结论 / 自己工具链缺陷当成被测对象缺陷 / 防缓存手段未经验证就当作"干扰已排除"）；速查索引补 3 行；总纲 25 → **28 条**并补**第五个变体**「把自己的测量或自己的工具当成客观事实」——三条同出于一次实测，说明测量类工作需独立于「得出数字」的复核环节；关联新增 [[本地LLM部署与显存实测-4GB卡]] |
| 2026-09-22 | 录入 **C-031~C-032**（一条判据捆了两件事导致归因反转；凭本地旧副本断定远端未修复，方向相反地误报了"其实做对的事"）|
| 2026-09-22 | 录入 **C-030**（拿被自己污染的实验得出的负结果当结论，且已写入交付物并推送到公开远端）。这是 C-025 的**第三次复现**——前两次污染的只是数据，这次**产出了一个错误结论**。速查索引补 1 行；总纲 29 → **30 条** |
| 2026-09-19 | 录入 **C-029**（**同一故障连续三次误判**：3B 模型能力 → `max_completion_tokens` 字段 → `maxTokens` 被工具开销吃光；真因是 `contextWindow` 小于 harness 的提示词估算）。速查索引补 1 行；总纲 28 → **29 条**。核心可复用规则：**诊断时一次只改一个变量、改完复测它是否真的影响了目标值、没影响就撤回别叠加**；突破口是「对比两次工具数不同的录包」——**做对照比做推理快**。三条误判各自都"自洽且有说服力"，与 C-023 同族 |
| 2026-09-13 | **撤回 C-020 的因果推断**（"缺标签导致提交未被处理"是错的：workflow 触发条件是 OR，标题以 `[Plugin]` 开头即成立，创建后 8 秒就已运行；静默失败机制仍为真），并在条目内加撤回说明与回链；补录真正根因 **C-021**（issue 表单是自动化接口，按 `### 字段名` 解析正文，自由 markdown 报 `SUBMISSION_FIELD_MISSING`）与 **C-022**（逐字节冻结的历史快照仍带 `dsh.bundle`，被遍历工具算成 7 个候选插件，报 `SUBMISSION_PACKAGE_AMBIGUOUS`）；速查索引补 2 行；总纲 20 → **22 条**；补记两条提交预检 `passed` 的独立印证结果 |
