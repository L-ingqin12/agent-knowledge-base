---
title: 向量数据库与检索
aliases: [向量检索, VectorDB, ANN算法]
tags: [cs/db, cs, ai]
created: 2026-08-26
updated: 2026-09-13
status: review
source: 算法论文与官方文档共识（HNSW/Malkov、PQ/Jégou、DiskANN；FAISS/pgvector/Milvus/Qdrant 文档）；前沿演进标待确认
fetched_at: 2026-08-26
---

# 向量数据库与检索

> [!abstract] 定位
> AI 基础设施纵深：从 embedding 到 ANN 检索的完整链路——相似度度量、HNSW/IVF/PQ 三大族机制级拆解、过滤难题、混合检索融合，以及产品谱系与容量估算。是 [[RAG检索增强生成实战]] 与 [[agent-memory-context-knowledge-design]] L3 记忆层的底层支撑。

See also: [[CS-KB-Home]] · [[深度学习算法基础]] · [[RAG检索增强生成实战]] · [[GraphRAG知识图谱增强实战]]

## 一、问题定义

```
文档/内容 --embedding模型--> d 维向量(384~3072)
查询 q 同样编码 → 找库中与 q 最近 TopK   (精确 KNN O(n·d) 不可行→ANN 近似)
```

- **度量必须匹配训练目标**：cosine(归一后等价内积 IP)/L2——用错度量=召回率无声崩塌
- 质量指标：recall@k（ANN 结果 vs 暴力搜索真值的重合率）× QPS × 内存三角权衡

## 二、ANN 三大族机制级

### 1. HNSW（分层可导航小世界图）
- 思想=跳表×近邻图：上层稀疏长边快速俯冲，**layer0 全量节点**短边精搜
- 参数：M(每点邻居数,典型16–48)/efConstruction(建图候选队列)/efSearch(查询候选队列，召回主旋钮)
- 优点：高 recall 下 QPS 最强族；缺点：内存大(n×d 向量 + n×M 邻接表)、删除靠墓碑标记、图遍历随机访问 cache 不友好

### 2. IVF（倒排文件+粗量化）
- k-means 聚 n 个向量为 √n 量级 centroid；查询先取最近 `nprobe` 个簇内暴力扫
- 需训练；簇边界丢召回 → nprobe 调高换时间

### 3. PQ 乘积量化（压缩轴）
- 把 d 维切成 m 段，每段 k-means 到 256 码本 → 每向量 m 字节（如 768 维 fp32=3072B，m=12 → 12B，压缩 **256×**；经典例子是 SIFT 128 维 / m=8：512B → 8B = 64×）
- ADC 用查表近似距离；OPQ 先旋转再切分降量化误差；常作为 IVF 的二级(PQ 内积表)组合成 IVF-PQ
- 代价：量化误差直接吃 recall → 必须带真值回归测试
- **两段式检索是工程必需**：① 用压缩码（ADC/二值）取 top-N（N≫k）；② 用原始 fp32 向量或残差编码对候选重排取 top-k——只做①不做②，量化误差会直接吃掉召回。召回验收判据写成「同一查询集上与暴力检索结果的重合率（recall@k 曲线）」，而非只看压缩比。

> [!warning] 更正（2026-09-13）：原写「768 维/12B ≈ 64×压缩」——算术错位：768×4B=3072B，压到 12B 是 **256×**；64× 是经典 SIFT(128 维)/m=8 的数字（512B → 8B）。
> 来源：https://raw.githubusercontent.com/pgvector/pgvector/master/README.md （Scaling 一节：二值量化 "with re-ranking for search"）

> [!tip] 组合拳与磁盘派
> 实战多为 IVF+PQ 或 HNSW+SQ 标量量化(int8)。十亿级单机放不下走 DiskANN/Vamana 磁盘图(SSD 随机读友好布局)。

### 2A. HNSW 插入与搜索伪码走读（把参数落到动作上）

**层数抽样——为什么是负对数**：
```
ℓ = floor( −ln(uniform(0,1)) × mL ),  mL = 1/ln(M)
```
均匀数取对数翻转后：大部分样本挤在 0 附近、少数拖长尾——恰好复刻跳表的几何分布（P(层≥k)=(1/M)^k）。M=16 时每上一层节点数≈÷16，log₁₆(n) 层即可覆盖亿级 → **俯冲路径 O(log n) 跳**。

**插入算法（Algorithm 1 精义）**：
```
INSERT(q):
  ℓ ← 抽样层; ep ← 入口点(全局)
  # 阶段1: 从最高层到 ℓ+1, ef=1 纯贪心 —— 只找"每层的最近点"当电梯井
  for lc ∈ [top..ℓ+1]:  ep ← GREEDY(ep, q, ef=1)
  # 阶段2: 在 ℓ..0 每层真搜索 + 建边
  for lc ∈ [ℓ..0]:
      W ← SEARCH-LAYER(q, ep, efConstruction)   # 候选队列：构建期宽度是 efConstruction，不是 M
      neighbors ← SELECT-NEIGHBORS-HEURISTIC(W, M)
      for e in neighbors:
          add_edge(q↔e);                      # 双向
          if degree(e) > M_max:               # 超容量裁剪
              prune(e 的邻居, 保多样性)
      ep ← W
```

> [!warning] 更正（2026-09-13）：原伪码写 `W ← SEARCH-LAYER(q, ep, ef=M)`——构建期层内搜索宽度是 **efConstruction**（hnswlib 还会把它钳到 `max(ef_construction, M)`），M 只决定选边数量与度上限。三者分工：M/M_max 管图结构与内存，efConstruction 管构建质量与耗时，efSearch 管查询召回与延迟。
> 来源：https://raw.githubusercontent.com/nmslib/hnswlib/master/hnswlib/hnswalg.h 、https://raw.githubusercontent.com/nmslib/hnswlib/master/README.md

**SELECT-NEIGHBORS 启发式（HNSW 比 NSW 强的核心）**：
候选按距 q 升序遍历，保留条件="比已选者都更接近 q"；若某候选更接近**已选的某个点**而非 q，则丢弃。效果：边不会扎堆在同一方向，图保持可导航性——这就是"启发式保多样性"，也是调 M_max 时召回不塌的结构保证。

**微例 trace**（2D, 插入 q=(5,5)，层抽到 1；现有 A(0,0)/B(10,10) 在层1）：
```
阶段1: 层1 GREEDY from A → dist(q,A)=7.07 < dist(q,B)=7.07? 相等取先到 → ep=A
阶段2: 层0 SEARCH ef=M 找到 {C(4,4), D(6,6), A, B...}
       SELECT: C 距 q=1.41 保留; D=1.41 但 D 是否更靠近 C? dist(D,C)=2.83 > 1.41 → 保留;
       B=7.07 且 B 更接近 D(5.66) < 7.07 → 弃 B ✓ (防止长边冗余)
```

**efSearch 召回数学直觉**：beam 宽度 ef 决定"贪心误入局部最优后被拉回的能力"。经验线：recall@10 ≥ 0.95 需 ef ≈ 10–20×K，再往上 QPS 线性掉——先测 recall 曲线再定参，不要拍。

## 三、过滤检索难题（工程最常见坑）

- **pre-filter** 先按属性筛再 ANN：图结构被切断→连通性崩、召回跌
- **post-filter** 先 ANN TopN 再筛：候选里可能没几个过筛→结果稀疏
- 成熟方案：把标量条件并入图遍历(过滤感知 HNSW)/双通道取并集重排/为高频过滤维度单独建集合级索引——各产品实现差异大，选型时必测自家过滤模式（最新学术方案 ACORN 等**待确认**成熟度）

> [!warning] 残余复核（2026-09-13）：「ACORN 等**待确认**成熟度」**仍开放**。ACORN 是过滤感知图索引方向的学术方案，这里的「成熟度」等于「是否已在主流引擎落地、有无生产规模数据」——本机既无 Milvus/Qdrant/Weaviate 实例，也无其源码可读（离线手段覆盖不到），故不定论。判据：抓 ACORN 论文页与其官方实现仓库，确认是否有可复现实现与 benchmark 中的规模数据；再按本文 §五 产品谱系**逐个**（Milvus / Qdrant / Weaviate / pgvector / ES）核对官方文档或 issue 里是否已内置该变体——逐个查，不抽样，也不以「某产品已支持」推广到全族。

> [!success] 残余复核（2026-09-13 联网轮）：**ACORN 这条「待确认」已拆开定案一半**——「有无可复现实现」「是否已进主流引擎」两问取到官方级证据；「生产规模数据」与「其余产品的普遍落地」**仍未获佐证**。
> - **可复现实现：确认存在**。ACORN 有官方实现仓库（`stanford-futuredata/ACORN`），自述 "built on The FAISS Library in C++"；论文为 SIGMOD '24 / arXiv:2403.04871，自述在「prior benchmark datasets」与「complex multi-modal datasets」上做到 "2-1,000x higher throughput at a fixed recall"。注意这是**benchmark 口径，不是生产部署口径**。
> - **落地：Qdrant 已内置（官方级证据）**。Qdrant **v1.16.0（发布 2025-11-17）官方 release notes** 列有 **"Add ACORN-1 search method, accurate search over many filtered points at the cost of performance"**（PR #7408，并回链官方文档）；官方 search 文档的参数表也把 `acorn` 列为搜索参数（"parameters related to the ACORN search algorithm"）。**但官方表述只到「有该方法/该参数」为止，未给采用度或生产规模数据**；且官方描述自带代价前提——**"at the cost of performance"**，即它是过滤场景下的**可选精度档**，不是默认路径。
> - **仍未获佐证的部分**：① 本文 §五 产品谱系里**其余产品**（Milvus / Weaviate / pgvector / ES）本轮**未取得官方 ACORN 落地证据**。已抽查 pgvector 官方 README（无 ACORN 字样）与 Qdrant 官方 README；其余线索来自第三方博客（称 Weaviate 用 BlockMax WAND、Milvus 用 bitmap 索引、pgvector 0.8 起用 iterative index scan），**均未在官方文档核验，故不作为依据**。② 「生产规模数据」（部署规模 / 案例数）ACORN 论文与 Qdrant 发布说明**均未给出**。
> - 本条最终口径：**ACORN 已有可复现实现，并已作为「过滤精度档」进入 Qdrant ≥1.16 的官方能力面；但「主流引擎普遍落地」与「生产规模数据」仍无官方佐证**。正文「各产品实现差异大，选型时必测自家过滤模式」的判断不变，只是「待确认」应具体化为「**除 Qdrant 外未见官方落地通报**」。
> 依据：https://arxiv.org/abs/2403.04871 ；https://github.com/stanford-futuredata/ACORN ；https://github.com/qdrant/qdrant/releases/tag/v1.16.0 ；https://qdrant.tech/documentation/search/search/ （取回于 2026-09-13）

## 四、混合检索与重排

```
BM25 关键词通道 ┐
                ├─▶ RRF 倒数排名融合 / 加权分数 ─▶ cross-encoder 重排 Top-k ─▶ 终榜
稠密向量通道    ┘
```

- 稀疏通道兜住专有名词/型号（embedding 弱项），稠密通道管语义改写
- RRF 公式 `Σ 1/(k+rank_i)` 免调权，k≈60 工程默认；重排器(cross-encoder)只对几十条精算，成本可控
- 本库映射：[[agent-memory-context-knowledge-design]] L3 的 wikilink 图谱+全文+向量三路混合即此范式；[[lognet-rootcause-multiagent-architecture]] 的 FTS5 通道对应 BM25 侧

## 五、产品谱系选型

| 层级 | 代表 | 定位 |
|------|------|------|
| 库 | FAISS / hnswlib / usearch | 嵌入自研，无服务化，GPU 支持看 FAISS |
| 关系库扩展 | **pgvector** / sqlite-vec | 复用现有 SQL 栈+事务+join 过滤；**千万级以内**最省心，到亿级必须走量化（halfvec/二值）+ 候选重排，或 Citus/PgDog 分片 |
| 专用引擎 | Milvus(存算分离/分段云原生)、Qdrant(Rust/负载过滤强)、Weaviate(模块化) | 十亿级/多租户/托管需求 |
| 搜索内核加列 | ES/OpenSearch dense_vector | 已有日志/搜索栈顺路加语义 |

> [!warning] 更正（2026-09-13）：pgvector 行原写「复用现有 SQL 栈+事务+join 过滤，亿级内首选省心」——与 §六「亿级 fp32 全量 HNSW 单机吃紧」自相矛盾，也超出官方口径：官方 Scaling 指引要求先用 halfvec / 二值量化并配候选重排，横向扩展另配 Citus/PgDog 分片；选型判据应是自家数据的 recall@k 与构建内存实测。
> 来源：https://raw.githubusercontent.com/pgvector/pgvector/master/README.md （Scaling / Monitoring 两节）

## 六、容量与成本速算

```
原始内存 ≈ n × d × 4B(fp32)      # 统一口径: 1 GiB = 2^30 B, 括号内为十进制 GB
10M × 768d = 28.6 GiB(30.7 GB)；int8 SQ 后 ≈7.2 GiB(7.7 GB)；PQ(m=24) ≈0.22 GiB(0.24 GB)+码本
HNSW 邻接表额外 ≈ n × M × 2 × 4B (M=32 时 10M≈2.4 GiB / 2.6 GB)
结论: 亿级 fp32 全量 HNSW 单机吃紧 → SQ/PQ 或 DiskANN 必选其一
```

> [!warning] 更正（2026-09-13）：原三行混用单位——「10M × 768d = 28.6 GB」实际是 28.6 GiB（=30.72 GB），而同一段的 PQ 0.24GB、邻接表 2.6GB 是十进制 GB，读者核算时对不上。现统一给出 GiB 与 GB 两个口径。

## 七、待确认项

> ① 各托管服务(filtered-HNSW 变体)的召回基准可比性（无统一定标集）；② Matryoshka 嵌入(截断维度分级检索)在主流 embedding 服务的产品化程度；③ GPU 图索引(RAPIDS RAFT)生产案例规模。

> [!warning] 残余复核（2026-09-13）：三条**均仍开放**——都要公开基准或厂商文档，本机离线无从取得（无向量库实例、无 GPU 可跑 GPU 索引），逐条给判据。
> - ① 判据：需要一份**带定标集**的横向对比（同一数据集 + 同一过滤模式下比 recall@k）。若确认不存在统一定标集，结论就应老实写成「当前不可比」，而不是拼一张排名表——本条的「可比性」本身就是结论，不必强行给出胜负。
> - ② 判据：逐个抓主流 embedding 服务文档中「输出维度可调/可截断」的参数（如 OpenAI `text-embedding-3-*` 的 `dimensions`），记录「是否支持、支持到多少维、官方宣称的精度损失口径」三列，再核对是否注明 Matryoshka 训练方式。
> - ③ 判据：抓 RAPIDS RAFT / cuVS 官方博客与文档里的生产案例页，记录最大规模、部署形态（单机多卡 / 多机）与索引类型；「生产案例规模」要的是案例数，不是 benchmark 峰值。

> [!success] 残余复核（2026-09-13 联网轮）：**①②③ 三条均已结**。其中 ③ 的结论是**否定性事实**（官方确实不提供生产案例数据），不是「没查到」。
> - **① 已结（口径层面）**：本条要的「可比性」答案是**基准的可用条件**，而不是「无基准」。公开 harness 确实提供 filtered search 场景与过滤档位——Zilliz 主导的 **VectorDBBench** 自述 "provides repeatable workloads for insertion, vector search, **filtered search**, and full-text search"，并提供 `LabelFilterPerformanceCase` / `NewIntFilterPerformanceCase`，参数含 **`--label-percentage`** 与 **`filter-type`（`post_filter` | `pre_filter` | `efficient_filter`）**。**但**「各托管服务官方博客数字之间可比」仍然**不成立**——各家发布的结果通常不在同一数据集档、同一过滤比例、同一过滤策略下。故口径定为：**跨服务比较必须落在同一 harness、同一 `dataset-with-size-type`、同一 label 比例、同一 `filter-type` 下取 recall@k；否则一律记「不可比」，不拼排名表**。另需注明该 harness **由 Milvus 厂商主导**，不是中立第三方定标集。
> - **② 已结**：
>   - **OpenAI**：官方公告写明 `text-embedding-3` 两个模型「were trained with a technique^A」——注释 A 即 **Matryoshka Representation Learning**（链到 MRL 论文）；并说明「developers can **shorten embeddings** … by passing in the **`dimensions`** API parameter」。精度损失给的是**具体对比口径**："on the MTEB benchmark, a `text-embedding-3-large` embedding can be shortened to a size of **256** while still outperforming an unshortened `text-embedding-ada-002` embedding with a size of **1536**"，并明说这是 "trading off some accuracy in exchange for the smaller vector size"。
>   - **Cohere**：官方 API reference 提供 **`output_dimension`**，"only available for **embed-v4 and newer** models. Possible values are **256, 512, 1024, and 1536**. The default is **1536**"——**但该页未出现 "Matryoshka" 一词**，即只能证明「输出维度可截断」这一产品能力，不能证明其训练方式。
>   - 结论：**可截断输出维度已是主流厂商的正式 API 能力**；但「是否明示 Matryoshka 训练」各家口径不一（OpenAI 明示、Cohere 未提）——不要把「有 `dimensions` 参数」等同于「用了 MRL」。
> - **③ 已结（否定性结论）**：**RAPIDS RAFT / cuVS 官方材料只给 benchmark 与集成清单，不提供生产案例数或部署规模**。依据：(a) cuVS 文档已迁至 `docs.nvidia.com/cuvs/`（旧 `docs.rapids.ai/api/cuvs/…` 路径 404）；(b) 其 integrations 页是**厂商集成清单**（Amazon OpenSearch Service、Elasticsearch、Milvus、Kinetica、Lucene 及数家存储平台），其中 **CyborgDB 被明确标为 "proof of concept"**，而出现的规模数字来自**第三方的演示**（如某存储厂商的 4.8B 向量 scaling demo），**不是 NVIDIA 的生产案例**；(c) NVIDIA 开发者博客相关文章只讲 IVF-Flat / IVF-PQ 等索引用法与 RAFT 索引实现，**无生产部署计数或规模声明**。另避免混淆：cuVS 确有官方 `cuvs.neighbors.tiered_index` API，但那是 **GPU/主机内存的索引分层**，**不是多机部署分层**。
> - 三条汇总口径：**① 可比性 = 「同 harness、同数据集档、同过滤比例、同策略才可比」；② 可截断维度已产品化（MRL 训练方式以 OpenAI 明示为准，Cohere 未声明）；③ GPU 图索引官方只有 benchmark，无生产案例规模数据**。
> 依据：https://github.com/zilliztech/VectorDBBench ；https://openai.com/index/new-embedding-models-and-api-updates/ （openai.com 对直连返回反爬页，经 Wayback 取回）；https://docs.cohere.com/reference/embed ；https://docs.nvidia.com/cuvs/getting-started/integrations.md ；https://docs.nvidia.com/cuvs/getting-started/use-cases.md （取回于 2026-09-13）

## Related

[[CS-KB-Home]] · [[RAG检索增强生成实战]] · [[agent-memory-context-knowledge-design]] · [[深度学习算法基础]] · [[Redis原理与实践]]

## 补完记录（2026-09-13）

| 类型 | 原问题 | 处置与依据 |
|---|---|---|
| 纠错 | §二 PQ 写「(如 768 维/12B ≈ 64×压缩)」 | 算术错位：768×4B=3072B 压到 12B 是 **256×**；64× 属经典 SIFT(128 维)/m=8 例子。改为 256× 并保留 SIFT 例；依据算术与本节「m 字节/向量」口径 |
| 纠错 | §2A 插入伪码写 `W ← SEARCH-LAYER(q, ep, ef=M)` | 改为 `efConstruction`，并补 M / efConstruction / efSearch 三者分工；依据 hnswlib `hnswalg.h`（`ef_construction_ = std::max(ef_construction, M_)`）与 README |
| 纠错 | §五 pgvector「亿级内首选省心」与 §六「亿级 fp32 单机吃紧」自相矛盾，且超出官方口径 | 改为「千万级以内最省心；到亿级必须量化（halfvec/二值）+ 重排，或 Citus/PgDog 分片」；依据 pgvector README Scaling 节 |
| 补疏漏 | §二 PQ 只写「量化误差直接吃 recall」，没给补救手段 | 补两段式检索（压缩码取 top-N → 原向量/残差重排取 top-k）与 recall@k 验收判据；依据 pgvector README「with re-ranking for search」 |
| 纠错 | §六 容量速算里 GiB 与 GB 混用（28.6「GB」实为 GiB，而 0.24GB/2.6GB 是十进制） | 统一标注两个口径（28.6 GiB=30.7 GB、7.2 GiB、0.22 GiB、2.4 GiB）并在表头注明 1 GiB=2^30 B |
| 残余复核 | §三「最新学术方案 ACORN 等**待确认**成熟度」（正文显式未决标记） | 判定**仍开放**：成熟度=是否落地主流引擎，本机无 Milvus/Qdrant/Weaviate 实例与源码可读；判据=抓 ACORN 论文与官方实现仓库的规模数据，再按 §五 产品谱系逐个核对是否已内置该变体（不抽样、不由个别产品外推全族） |
| 残余复核 | §七 待确认①②③（托管服务召回基准可比性 / Matryoshka 产品化程度 / GPU 图索引生产案例规模） | 逐条判定**仍开放**并各给判据：① 无统一定标集时结论应写作「不可比」而非拼排名；② 逐个抓 embedding 服务文档的可截断维度参数（如 `text-embedding-3-*` 的 `dimensions`）三列对比；③ 抓 RAPIDS RAFT/cuVS 官方生产案例页取案例数与部署形态。三条均需外部输入，本机无实例/无 GPU |
| 残余复核（联网轮） | §三「最新学术方案 ACORN 等**待确认**成熟度」（正文显式未决标记） | **拆开定案一半**：可复现实现确认存在（官方仓库基于 FAISS，SIGMOD '24 / arXiv:2403.04871，收益为 benchmark 口径）；落地取到**官方级证据仅 Qdrant**——v1.16.0（2025-11-17）release notes 与官方 search 文档均列 ACORN-1/`acorn` 参数，且官方自述 "at the cost of performance"（可选精度档）。仍开放：其余产品（Milvus/Weaviate/pgvector/ES）无官方 ACORN 证据（第三方说法未采信）；生产规模数据论文与发布说明均未给。依据：https://github.com/qdrant/qdrant/releases/tag/v1.16.0 ；https://qdrant.tech/documentation/search/search/ ；https://arxiv.org/abs/2403.04871 |
| 残余复核（联网轮） | §七 待确认①②③（托管服务召回基准可比性 / Matryoshka 产品化程度 / GPU 图索引生产案例规模） | **三条均结**：① 可比性口径=「同 harness、同 `dataset-with-size-type`、同 `--label-percentage`、同 `filter-type` 才可比」，因 VectorDBBench（Zilliz 主导）已提供 filtered search 场景与 pre/post/efficient 三档过滤，但它是厂商主导 harness 而非中立定标集；② OpenAI 官方明示 `text-embedding-3` 用 MRL 训练且 `dimensions` 可截断（MTEB 口径：3-large 截到 256 仍强于未截断 ada-002 的 1536），Cohere `output_dimension`（embed-v4+，256/512/1024/1536，默认 1536）**未提 Matryoshka**；③ **否定性结论**——cuVS/RAFT 官方只有 benchmark 与厂商集成清单（CyborgDB 自标 "proof of concept"），无生产案例数或部署规模，且 `cuvs.neighbors.tiered_index` 是内存分层非部署分层。依据：https://github.com/zilliztech/VectorDBBench ；https://openai.com/index/new-embedding-models-and-api-updates/ ；https://docs.cohere.com/reference/embed ；https://docs.nvidia.com/cuvs/getting-started/integrations.md |

回链：[[CORRECTIONS]] · [[AGENTS]]
